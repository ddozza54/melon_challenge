extends baseNav
include mixins/song

block content
    div.songList
        each song in songs
            +song(song)
        else
            li Sorry nothing found


block scripts
    script(src="/static/js/musicHome.js") 