extends baseNav
include mixins/song

block content
    if(errorMessage)
        p=errorMessage
    div.songList
        each song in songs
            +song(song)
        else
            p 플레이리스트가 없습니다. 👍를 눌러서 노래를 추가해보세요.