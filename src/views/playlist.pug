extends baseNav
include mixins/song

block content
    if(errorMessage)
        p=errorMessage
    div.songList
        each song in songs
            +song(song)
        else
            li Sorry nothing found